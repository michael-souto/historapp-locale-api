CREATE TABLE LOCALE (ID  BIGSERIAL NOT NULL, COMMENTS VARCHAR, CREATEAT TIMESTAMP NOT NULL, LAT INT8, LNG INT8, NAME VARCHAR NOT NULL, TAGS VARCHAR, UPDATEAT TIMESTAMP, VISIBILITY INT8, PRIMARY KEY (ID))